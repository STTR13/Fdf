#include "../includes/fdf.h"

char **array_maker(char *str)
{
	int x;
	int y;
	int
	x = 0;
	y = 0;
}

char	**file_reader(int fd)
{
	char *str;
	char *buffer
	int counter

	while (counter = read(fd, buffer, 1000) > 0)
	{

	}


	return (array_maker(str));
}
