#include "../includes/fillit.h"

int main(int argc, char **argv)
{
  t_map *input;

  argv = NULL;
  input = NULL;
  if (argc != 2)
  {
    ft_putstr("usage: fillit target_file\n");
    return (1);
  }
  return (0);
}
